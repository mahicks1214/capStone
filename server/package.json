{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "knexfile.js",
  "scripts": {
    "start": "knex migrate:rollback && knex migrate:latest && knex seed:run && nodemon Api.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^2.5.1",
    "node": "^20.6.0",
    "nodemon": "^3.0.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2"
  }
}
